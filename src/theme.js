import { themeColors } from './theme/colors';
import { themeForms } from './theme/forms';
import { themeImages } from './theme/images';
import { themeMessages } from './theme/messages';
import { themeRoutes } from './theme/routes';

export const name = 'Theming File';
export const colors = themeColors;
export const messages = themeMessages;
export const images = themeImages;
export const routes = themeRoutes;
export const forms = themeForms;
